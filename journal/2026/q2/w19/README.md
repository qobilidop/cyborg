# 2026 W19 (May 4–10)

Return-to-coding week after the W18 busy-work-period. Mon a brief check-in, then Tue–Wed delivered four substantive z3wire PRs in two days (construction API redesign #21, Bazel CI ~30× speedup #22+#23, LE/BE byte API #24, naming-rule restatement + first symex example #25). Sun closed the week with two more shipping sessions: comparison-semantics tightening (PR #27, strict matching width+signedness on `==`/`!=`/`<`/`<=`/`>`/`>=` + new `math_eq`/`math_ne`/`math_lt`/`math_le`/`math_gt`/`math_ge` heterogeneous free-function family) and structured-view demo `examples/packet_view.cc` (PR #29, 4 class templates IPv4/Eth/Hdr views). Issue #28 (Bazel CI slowness) filed Sun s01 and extensively investigated Sun s02 — main is fully cached (`internal` ≠ miss; protobuf "Compiling" logs are replayed stderr); PR slowness from 249 fresh abseil + protobuf-for-tool actions (hypotheses: BuildBuddy retention eviction OR dev container layer hash drift). Days 05-07 through 05-09 not recorded.

## Days

- [05-04-mon](05-04-mon/README.md): Brief return-to-coding check-in with Gemini after busy off-period — no substantive work
- [05-05-tue](05-05-tue/README.md): z3wire construction API redesign (PR #21, `FromValue`/`FromExpr` → `Literal`/`From`/`TryFrom` uniform triple, 12 commits) + Bazel CI speedup (PR #22 persistent disk cache + committed `MODULE.bazel.lock`, ~1h14m → ~2m32s warm-cache, ~30×; PR #23 cast-roundtrip fuzz test)
- [05-06-wed](05-06-wed/README.md): Gemini Q on OxCaml dependent types (no — Layouts/modal types but no dep types, GADT+Peano workaround) + z3wire LE/BE byte API (PR #24, explicit endianness, deletes wide `value()` overload) + 3-bucket naming rule restatement direct-to-main + first symex example `exact_match_table.cc` (PR #25, requires `constexpr operator==`)
- [05-10-sun](05-10-sun/README.md): z3wire comparison semantics redesign (PR #27, strict `==`/`!=`/`<`/`<=`/`>`/`>=` matching width+signedness + new `math_*` free-fn family for heterogeneous; 6-task additive→migrate→tighten plan, ~24 subagent invocations) + structured-view example `examples/packet_view.cc` (PR #29, 4 class templates for IPv4/Eth headers over `SymUInt<BufW>*` buffer) + issue #28 CI investigation (main fully cached; PR-only 249 fresh abseil+protobuf actions; BuildBuddy retention vs dev container layer hash drift hypotheses)

## Agent index

- Z3WIRE-PRS-LANDED-W19: #21 (construction API redesign), #22 (Bazel disk-cache CI), #23 (sym_bit_vec_cast fuzz test), #24 (LE/BE byte API + style "accessor vs non-accessor"), #25 (`constexpr operator==` + `exact_match_table.cc` symex example), #27 (comparison semantics strict + `math_*` family), #29 (`packet_view.cc` structured-view example) + direct-to-main: `b9bc2f8` roadmap cleanup, `0bc471e` style.md naming-rule restatement
- Z3WIRE-PUBLIC-API-SHAPE-NOW: per-type triple `Literal<V>()` (NTTP, static_assert) / `From(v)` (abort via Z3W_CHECK) / `TryFrom(v)` (recoverable — concrete returns named struct, symbolic returns `optional`). Byte API on `BitVec<W,S>`: `FromLeBytes`/`FromBeBytes`/`TryFromLeBytes`/`TryFromBeBytes` factories + `ToLeBytes()`/`ToBeBytes()` members. Wide `value()` overload removed; narrow `value()` returns native int. `BitVec::operator==` is `constexpr` (W>64 branch uses non-constexpr `ToLeBytes` but C++20 allows non-constexpr in unreachable branches during constant evaluation). **W19 Sun additions:** `SymBitVec<W,S>` comparison operators `==`/`!=`/`<`/`<=`/`>`/`>=` now strict (static_assert matching width AND signedness); heterogeneous math via free fns `z3w::math_eq`/`math_ne`/`math_lt`/`math_le`/`math_gt`/`math_ge`. Concrete `BitVec` and `Bool`/`SymBool` untouched
- Z3WIRE-COMPARISON-DESIGN-RATIONALE (05-10 s01): old behavior mathematically correct but silently traversed type boundaries; conflicted with library's "explicit over implicit" principle (bitwise already strict, arithmetic uses bit-growth). `SymUInt<8>(255) == SymSInt<8>(-1)` is `false` mathematically vs `true` bit-pattern. Cross-lang precedent: Rust/Go/Haskell/Zig strict; Swift heterogeneous-for-all-six has documented overload-res + integer-literal-overflow problems. C++20 `std::cmp_*` is direct precedent for `math_*` family
- Z3WIRE-PACKET-VIEW-DESIGN (05-10 s02): 4 class templates `(int BufW, int BaseOffset)` holding `z3w::SymUInt<BufW>* buf_`. `Ipv4AddrView` (full 32 bits + `octet0..3`), `Ipv4View` (leaf + `src_addr`/`dst_addr` sub-views at local offsets), `EthView` (leaf), `HdrView<BufW>` composite (no own `buf_`, `static_assert(BufW >= 432)`). Local LSB-relative bit indices. Cousins: Chisel `Bundle`, SystemVerilog packed structs, P4 headers, CMSIS register access. API style: explicit methods, no proxies/operator= magic; leaf `field()/set_field()` + sub-view `value()/set_value()` asymmetry accepted
- Z3WIRE-NAMING-RULE-FINAL: 3-bucket positive form in `docs/dev/style.md`. (1) Free fns: snake_case (matches Z3 `z3::shl/ashr/concat/ite` + std:: + SystemC + Bitwuzla). (2) Class methods: Google C++ Style PascalCase if non-accessor (incl. instance methods like `ToLeBytes`), snake_case if accessor (`value()`/`expr()`). (3) Type traits: std-style snake_case + `_v`. Sharpened from earlier "static vs instance" framing. Audit found camelCase counterexamples (Verilator/MLIR/CVC5) so claim narrowed to "matches Z3 + std::" only. Snake_case-everywhere refactor deferred (`true_`/`false_` per Boost would be load-bearing concession)
- Z3WIRE-NODISCARD-CONVENTION (05-10 s02): state-returning accessors get `[[nodiscard]]` (e.g. `z3w::SymBitVec::expr()`). Enforced by clang-tidy `modernize-use-nodiscard` as warning-as-error. Phase 3 lint surfaced 12 misses → bake into plan from Phase 1
- BAZEL-CI-SPEEDUP-PATTERN: persistent disk cache via `actions/cache@v4` keyed on `hashFiles('.bazelversion', 'MODULE.bazel.lock', '.bazelrc')`. **Lockfile must be committed** or `hashFiles` returns empty-set hash → cache key never invalidates → silent staleness. Bazel 8+ official guidance is to commit Bzlmod lockfiles. After warm: BuildBuddy round-trips eliminated. Repeatable pattern for similar Rust/CMake projects
- BAZEL-CI-DEP-CACHE-MISS-INVESTIGATION (05-10 s01+s02): main post-#27 fully cached (`245 disk + 916 remote + 1287 internal` = 0 fresh). **Critical insight:** `internal` ≠ cache miss — means "ran without subprocess" (symlinks, header scans); protobuf "Compiling" lines on main are Bazel **replaying cached stderr**, not fresh compile. PR #27: 249 fresh actions = abseil + protobuf-for-tool deps (not Z3Wire's own code). Two hypotheses: (1) BuildBuddy retention eviction between last main run + PR; (2) dev container layer hash drift (workflow builds per-run, only base layers cached via `cacheFrom`) causing action-hash drift. Next: BuildBuddy "Compare invocations" UI on PR-run vs same-day main-run — same-action different-hash = (2), same-hash but PR-miss = (1)
- WORKTREE-LEAVES-BRANCH-PATTERN: `ExitWorktree(action: "remove", discard_changes: true)` removes worktree dir but leaves local branch — needs follow-up `git branch -D <name>`. Confirmed in 2/2 cases on Tue, again on Wed and Sun. `EnterWorktree` branches off `main` (not current HEAD), auto-prefixes `worktree-`, substitutes `/` → `+` (rename via `git branch -m feat/<name>` post-creation each time)
- WORKTREE-SQUASH-MERGE-CLEANUP-PATTERN: project uses GitHub "Squash and merge". After merge, feature-branch commits unreachable from new main → `git branch -d` refuses (`-D` required). `ExitWorktree(action: "remove")` needs `discard_changes: true` for same reason. User confirms merge before agent proceeds with cleanup. `Discarded N commits` is expected on squash-merged branches
- WORKTREE-SCRATCH-DOES-NOT-PROPAGATE (05-10 s01): `.agent_scratch/` is gitignored, so new worktrees start without spec/plan files — `cp` from main checkout each time
- WORKTREE-BAZEL-WILDCARD-QUIRK (05-10 s01): worktree symlink (e.g. `bazel-comparison-semantics`) breaks `bazel //...` wildcard. Use explicit `//z3wire/... //examples/... //tests/...`. Known pre-existing quirk
- USER-FEEDBACK-WORKTREE-VS-DIRECT: confirmed multiple times — worktree+PR for non-trivial work, skip-worktree-skip-PR direct-to-main for trivial single-file docs/config edits (used Tue for `b9bc2f8` roadmap cleanup, Wed for `0bc471e` style restatement). Saved to `~/.claude/projects/-Users-qobilidop-i-z3wire/memory/feedback_skip_worktree_for_trivial.md`
- USER-FEEDBACK-SEPARATE-CI-STEPS: pushed back on collapsing Build + Test into single devcontainers/ci step (Tue). Reasons: per-step duration metrics, separate BuildBuddy invocation IDs, per-step pass/fail visibility in Actions UI, doc symmetry. 1-3 min container-setup overhead one-time per run; visibility benefits recurring forever
- USER-FEEDBACK-FULL-WORDS: full words over abbreviations (`symbolic_execution/` not `symex/`)
- USER-FEEDBACK-PR-TEST-PLAN-VERIFIED (05-10 s02): verify every PR test plan item locally before opening, mark `[x]` for passing. User caught me leaving boxes unchecked. Saved to `~/.claude/projects/-Users-qobilidop-i-z3wire/memory/feedback_pr_test_plan_verified.md`
- PROCESS-WIN-SKILL-PIPELINE: `superpowers:brainstorming` → `writing-plans` → `subagent-driven-development` → `finishing-a-development-branch` worked end-to-end on PR #21 (12 implementer + 11 reviewer subagent dispatches, all sonnet), PRs #24, #25, **PR #27 (6 plan tasks × {implementer + spec-review + code-quality + occasional fix} ≈ 24 dispatches; two-stage review caught real issues every task), PR #29 (3 implementer phases + spec-compliance + code-quality reviews each)**. Long brainstorming reshapes designs. Spec/plan in gitignored `.agent_scratch/` (AGENTS.md mandate)
- PROCESS-LESSON-VERIFY-API-BEFORE-CLAIMING (Wed s02): grep before asserting alternatives. Claude misclaimed `extract` was template-only when proposing raw-Z3 escape; user knew the symbolic-offset `extract<TargetWidth>(val, start_idx)` overload existed at `sym_bit_vec.h:543`
- PROCESS-LESSON-AUDIT-OVERCLAIM (Wed s02): always verify when persuasion case rests on "everyone does it." Self-corrected "ecosystem matches snake_case" claim mid-stream — web search found CVC5/Bitwuzla/Verilator camelCase counterexamples, narrowed to "Z3 + std::" only
- PROCESS-LESSON-IMPLEMENTER-DEVIATION-CORRECT (Tue s01): in `to_concrete`, implementer used `TryFrom(...).value` instead of plan's `From()` because Z3's `get_numeral_uint64()` returns bit pattern as unsigned (signed negative would spuriously abort `From()`). Trust subagent judgment when reasoning sound
- PROCESS-LESSON-PLAN-MISS-IMPLEMENTER-CAUGHT (Wed s01): plan said fuzz tests didn't need changes, but `Type<W>::ValueType` is byte-array for W>64 — renaming `TryFrom(span)` → `TryFromLeBytes` broke compilation. Implementer added `TryFromValue` helper in 3 fuzz files (file-local anonymous namespace) dispatching on width. Internal-only test plumbing
- PROCESS-LESSON-CROSS-TYPE-CALL-SITE-MIGRATION-GAP (05-10 s01): planning-time regex (`SymUInt<N>.*SymUInt<M>` on same line, literal `operator==`) missed many sites. Tasks 4+5 had to migrate 3 and 4 additional test sites respectively. Mitigation: bake "surface unexpected sites in build step" into plan
- PROCESS-LESSON-FORWARD-REFERENCE-COMMIT-TRAP (05-10 s01): comments asserting "operators are strict" landed in commits *before* operators became strict (additive phase). Reviewer caught. Fix: phrase comments truthfully at every commit on branch, or omit and let section header carry it
- PROCESS-LESSON-DOC-TITLE-RENAME-RIPPLE (05-10 s01): renaming `mathematical-comparison.md`'s *title* (file kept name) required updating `docs/design/index.md` display name + cross-link display text in `bit-growth-arithmetic.md`, `concrete-types.md`. Always grep for old title strings
- PROCESS-LESSON-DRAFT-COMMENT-RETRACTION (05-10 s02): first draft of issue #28 comment dismissed user's dep-rebuild intuition; had to retract and revise. User correctly intuited. Lesson: don't dismiss user's instinct without verifying
- LSP-CLANGD-NOISE-Z3WIRE: clangd lacks Bazel include paths. "File not found" / "unknown identifier" diagnostics throughout — all spurious. Bazel/CMake build is source of truth. Recurring across all sessions touching headers
- DEV-CONTAINER-VS-HOST-Z3WIRE: dev container has prebuilt Z3 4.15.2 at `/usr/local/lib/libz3.so` (`.devcontainer/Dockerfile:20-37`). CMake uses via `find_library`. Bazel builds Z3 from source via `bazel_dep(name = "z3", version = "4.15.2")` in `MODULE.bazel:14` — keep versions in sync. C++ toolchain NOT hermetic (`cc_configure` autodetects host clang)
- BRANCH-PROTECTION-MAIN-Z3WIRE: NONE — direct push works. No "linear history" toggle either, worth enabling since project squash-merge-only — out of scope
- CO-AUTHOR-CREDIT-CONVENTION: per `docs/dev/workflow.md` "For agents: credit yourself ... for commits you made" — implementer subagents (Sonnet 4.6) should self-credit their actual model, not copy a template's hardcoded "Claude Opus 4.7 (1M context)". Controller (Opus 4.7) credits self. Don't normalize across actors
- AMEND-VS-NEW-COMMIT-RULE (05-10 s01+s02): trivial fixups `--amend --no-edit` pre-push (used freely throughout per-task review cycles). NEW commit post-push (e.g. "Fix formatting per CI") to avoid force-push to remote
- LOCKFILE-CHURN-BENIGN: if dep-bump PR didn't include new lockfile entries (e.g. #16 for `rules_shell` 0.8.0), they appear on next fresh build. Commit as a chore alongside other work or separately (used in PR #25)
- MDFORMAT-ZENSICAL-ESCAPE-GOTCHA: `mdformat` wrapping `(W <= 64)` across line break inserts `\<` which Zensical doesn't consume as CommonMark escape — renders literal backslash. Workaround: rephrase to avoid `<=` symbol. If recurs, check generated HTML for literal backslash-then-entity
- AUTO-FORMAT-COLUMN-LIMIT-WORKAROUND (05-10 s01): clang-format col limit 80 wrapped `// --- Mathematical equality (heterogeneous: any width/signedness combination) ---` ugly. Workaround: shorten to `(heterogeneous)`, move detail to per-function docstrings
- COMPILE-FAIL-TEST-PATTERN (05-10 s01): compile-fail tests expect substring `"comparison requires matching width and signedness"`; `tests/compile_fail/BUILD.bazel` uses fixed-string match
- CONSTRUCTOR-INIT-ORDER-CPP (05-10 s02): C++ rule — init order must match member declaration order regardless of init-list order. `-Wreorder-ctor` only surfaces on template instantiation. Bazel cache hit after cosmetic fix was so good that build output didn't reveal whether fix took effect — trust C++ language semantics, verify with spec reviewer
- OXCAML-NO-DEPENDENT-TYPES (Wed s00): Jane Street's OCaml extensions add Layouts (memory unboxing/SIMD) + modal types (concurrency, allocation) + polymorphic params/labeled tuples/immutable arrays — no dependent types. For N-bit types still need GADTs + Peano numerals (works for small known widths; unwieldy at 32-bit+). Idris/Agda/Coq/F* for true dependent types
- ISSUE-28-OPEN-CI-INVESTIGATION (05-10 s01+s02): proposals (1) BuildBuddy "Compare invocations" PR-vs-main diff (highest leverage, 30-90 min); (2) tag `tests/fuzz/*` cc_tests `manual` so `bazel build //...` skips them (eliminates protobuf from default graph, ~30 min); (3) nightly pre-warm cache (only useful if hypothesis 1). Earlier "split sym_bit_vec.h" angle **retracted** — cascade is at dep layer not z3wire headers
- OPEN-FOLLOWUPS-Z3WIRE: (1) Doc note: explicit "unlike Rust's `From`, ours aborts" sentence in `docs/usage/types.md`. (2) Simplify `if constexpr (W > 64)` dispatch in `operator==` and `bit_vec_checked_test.cc::VerifyCheckedIdempotent` (now redundant since byte API uniform across widths). (3) Collapse `TryFromValue` helper in 3 fuzz files to direct `TryFromLeBytes` (or inline `if constexpr` into macro body). (4) Snake_case-everywhere refactor (renames `From`/`TryFrom`/`Literal`/`True`/`False` + new `*LeBytes`/`*BeBytes`). (5) `saturating_cast<T>(v)` as explicit op if real use case appears. (6) Node 20 deprecation on `actions/cache@v4` (forced to Node 24 on 2026-06-02; removed 2026-09-16) — affects both `bazel.yml` and `cmake.yml`. (7) Out-of-scope from #27: concrete heterogeneous `math_*`; new ordered comparison operators on concrete `BitVec` (only `==` exists; `!=` via C++20 rewriting); native-int comparison (`x == 0`); compile-fail tests for `!=`/`<=`/`>`/`>=` (only `==` and `<` have dedicated tests, others reuse static_assert mechanism). (8) Issue #28 follow-up. (9) Phase 1 review on `HdrView` had unused `buf_` — dropped in Phase 2
- ROADMAP-OPEN-Z3WIRE: ops (division/remainder, repeat, reduction ops, SymUInt<1>/SymBool friction); release (v0.1.0 tag, BCR publish, vcpkg port); ergonomics (solver-interaction usage page, native-int comparison promotion, symbolic struct construction helpers, saturating_cast); quality (test coverage — signed under-tested, W=1/W=64 boundaries, mixed concrete+symbolic bitwise); fuzz (arithmetic properties commutativity/negation, shift properties `shl<N>(shr<N>())`, continuous-fuzzing CI workflow); future symex examples in `examples/symbolic_execution/` (ternary match, range match, LPM, multi-stage match-action pipelines)
