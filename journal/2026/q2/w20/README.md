# 2026 W20 (May 11–17)

Quiet week so far — only Thu recorded. Single deep-dive Gemini Q&A session on Python `yield` mechanics → state-machine translation, then symbolic execution engine architecture for user's project (2× 2000-bit BitVec store, 3 instruction types with branching): KLEE iterative loop + memory optimizations, Z3 expr cost model, KLEE solver chain (constraint independence, CEX cache, canonical query cache, stateless oracle), iterative DFS with sync'd `push`/`pop` (Peek-and-Mutate stack), Branch Descriptor pattern, recursive vs iterative trade-offs, isolated vs accumulated branch guards (Local vs Effective), "Flat Sibling" SMT pattern, stepper-returns-ordered-guards contract. Days 05-11 through 05-13 and 05-15 through 05-17 not recorded.

## Days

- [05-14-thu](05-14-thu/README.md): Daily log with Gemini 3.1 Pro — brief Python `yield`/`yield from` mechanics + mechanical state-machine translation; then long thread on symbolic execution engine architecture (KLEE internals, Z3 expr cost model, DFS+incremental solving alignment, Branch Descriptor pattern, recursive vs iterative DFS, Local vs Effective branch guards, Flat Sibling SMT pattern, stepper-priority-order contract)

## Agent index

- PYTHON-YIELD-MECHANICAL-TRANSLATION (05-14 s00): generator → class implementing iterator protocol. Locals→`self` attrs; `self.pc` tracks resumption; `__next__` is state dispatcher; `yield`→`return` after pc update; function end→`raise StopIteration()`. `yield from <expr>`→`while True: try: yield next(_iter); except StopIteration as e: ...; break`. Compiler does heavy lifting; strict formal equivalence
- KLEE-ARCHITECTURE (05-14 s00): single-process iterative Executor loop manages ExecutionState pool. Searcher (Random Path / Cover-New / Coverage-Optimized) picks next; per-iteration executes 1 LLVM instruction; on symbolic branch, fork creates new ExecutionState (NOT OS fork). Memory: object-level COW at MemoryObject granularity, persistent map for address→object mapping (O(log N) fork), pruning + dec-refcount on termination, expression sharing/internalization via ConstraintManager. Treats Z3/STP as **stateless oracle** (no incremental push/pop) — incompatible with heuristic search's LCA backtracking
- KLEE-SOLVER-CHAIN-OPTIMIZATIONS (05-14 s00): (1) constraint independence (slice path condition to vars touching branch); (2) CEX cache (cached models from prior calls plugged into new query → bypass Z3 if any satisfies); (3) canonical query caching via hash-cons → O(1) hashmap. Order matters: cheap pruning first, full `solver.check()` last
- Z3-EXPR-CHEAP-COPY-IF-SAME-CONTEXT (05-14 s00): `z3::expr` wraps `Z3_ast*` raw pointer (`shared_ptr`-like). Copy = O(1) ref-count bump. Hash-consing: structural equality = pointer comparison = O(1). **Fatal caveat:** `Z3_ast` bound to creating context; per-state separate contexts require `Z3_ast_translate` deep copy = bottleneck. **Recommendation:** single global `z3::context`, separate cheap `z3::solver` instances per state (or incremental push/pop). Watch ref-counted leaks (dead constraints retained = RAM climb)
- DFS-PEEK-AND-MUTATE-PATTERN (05-14 s00): explicit stack frame `{state, explored_true, explored_false}` (Python) or `{state, fetched_branches, branches, next_branch_index}` (C++ Branch Descriptor variant). **PEEK not pop** — parent anchors solver pop. `solver.push()` synced with stack.append; `solver.pop()` synced with stack.pop (only if non-empty after). Zero solver thrashing, max RAM = max tree depth (not path count)
- BRANCH-DESCRIPTOR-PATTERN (05-14 s00): stepper returns `std::vector<SymbolicTransition>` = `{z3::expr guard, std::vector<std::pair<VarID, z3::expr>> assignments, Address next_pc, optional std::function<void(state)> apply_effects}`. Stepper is read-only on state; engine decides feasibility via solver. Decouples instruction semantics from search strategy. Avoid C++20 coroutines for symbex (ownership/refcount nightmare). Used by Crucible (Galois Haskell, CPS), CBMC, Microsoft Boogie/Corral, Z3 SPACER. Maps cleanly to Lean 4/Rocq for verifying the engine itself (pure functions State→List[Transitions])
- STEPPER-EAGER-VS-LAZY-FORKING (05-14 s00): `vector<ExecutionState>` = eager (pays copy cost before SAT check; state needs `last_guard` field; logic coupled to fork()). `vector<SymbolicTransition>` = lazy (fork only after SAT). Lazy is purer (data-driven recipe), debuggable, refactor-friendly
- SYMBOLIC-STORE-PERSISTENT-DATASTRUCTURE (05-14 s00): C++ gold standard = arximboldi/immer HAMT for O(1)/O(log N) fork. Python Angr uses CoW dict-chain (O(1) fork, O(D) lookup). **Not relevant for user's 2× BitVec store** — copying 2 z3 pointers is O(1)
- PRUNING-PIPELINE-5-STAGES (05-14 s00): (1) constant folding in stepper (`cond.is_true()/is_false()` → return only feasible branch); (2) `z3::expr.simplify()` algebraic reduction; (3) constraint independence (var-overlap check); (4) CEX cache; (5) full `solver.check()`. Order strictly by cost. "Impossible guard is a gift" = Dead Code Elimination for execution tree
- RECURSIVE-DFS-PYTHON-VS-CPP (05-14 s00): Python — soft `RecursionError` at ~1000 (catchable, gracefully drop). C++ — hardware segfault (uncatchable) at OS stack: Linux/macOS main 8MB (~130k frames at 64B), threads ~512KB, Windows 1MB. **C++ requires manual `depth` counter** to fail gracefully. RAII syncs perfectly with Z3 refcounting in C++
- RECURSIVE-DFS-COPY-BOTTLENECK (05-14 s00): in recursive DFS, `state.copy()` triggers store duplication. Large `std::map`/`dict` (10k vars) = bottleneck. **Not user's concern** — 2× BitVec store = O(1) pointer copy. New watch: AST tree depth growth (sequential `concat`/`extract` chains) chokes solver on `.check()`
- BRANCH-GUARD-LOCAL-VS-EFFECTIVE (05-14 s00): isolated/local guard $I$ vs accumulated/effective guard $A$. $A_3 = \neg I_1 \land \neg I_2 \land I_3$ for fall-through chain. Synonyms: "Reachability Condition" (CFG), "Disjoint Path Constraint" (symex — overlapping I → mutually-exclusive A), "Priority Encoder" (HW), `EdgeCondition`/`PathConstraint` (classic symex). **Engine using only I-guards is UNSOUND** (`if x>10 else if x>5` would find x=20 satisfying I_2 but actually goes to block 1). **Recommended C++ names:** `LocalGuard`/`EffectiveGuard`
- FLAT-SIBLING-SMT-PATTERN (05-14 s00): **wrong** — `push I_1`→`pop`→`push !I_1`→`push I_2`… ("staircase") creates N nested scopes for N branches; CDCL hates deep scopes (lemma partitioning thrashing). **Right** — outer `solver.push()` for entire chain; per-branch: inner `solver.push()`+`add(local_guard)`→`check()`→recurse if sat→`solver.pop()`→`solver.add(!local_guard)` *into outer scope*. **Max stack depth = 2 regardless of chain length.** Z3 natively accumulates (flat `[!I_1, !I_2, ...]` enables unit propagation)
- STEPPER-CONTRACT-PRIORITY-ORDER (05-14 s00): steppers MUST return local guards in **priority order** (highest first, fall-through `true` last). Engine blindly applies Flat Sibling. Stepper owns Semantic Priority; engine owns Logical Accumulation. Fall-through MUST be present as final element with `true` guard. Hallmark of professional-grade formal verification tool
- INCREMENTAL-SOLVING-DFS-ALIGNMENT (05-14 s00): `push()`/`pop()` ideal for strict DFS; useless for heuristic search (KLEE/Angr). Engines using incremental: Z3 SPACER, CBMC, Crucible
- USER-PROJECT-SE-ENGINE-CONTEXT (05-14 s00): user building own symex engine. 3 instruction types each with branching. Symbolic store = 2× ~2000-bit BitVec. **Recommended architecture:** recursive C++ + Branch Descriptor pattern + `LocalGuard`/`EffectiveGuard` naming + Flat Sibling SMT pattern + stepper-priority-order contract + manual depth counter. State copy O(1) (2 pointers); risk is AST tree depth on `solver.check`, not memory
