# 2026-05-14 (Thursday)

Single daily log with Gemini 3.1 Pro: brief Python `yield`/`yield from` mechanics (with mechanical state-machine translation), then a deep symbolic-execution architecture deep dive — KLEE iterative loop + memory optimizations (object-level COW, persistent maps), Z3 expr ref-counting + single-context recommendation, KLEE solver chain (constraint independence, CEX cache, canonical query caching, stateless oracle), iterative DFS with sync'd `push`/`pop` (Peek-and-Mutate stack frame), Branch Descriptor / SymbolicTransition pattern for modular C++ steppers, recursive vs iterative trade-offs (Python soft RecursionError vs C++ hardware segfault), Local vs Effective branch-guard naming, "Flat Sibling" SMT pattern for if-else-if chains, stepper-returns-ordered-guards contract.

## Sessions

- **session-00**: Daily log with Gemini 3.1 Pro. Two threads. **Thread 1** (~19:13-19:16): Python `yield` example + mechanical translation to state machine (`pc` field, `__next__` dispatcher, `yield`→`return`, function end→`StopIteration`); `yield from` expansion to while-loop over sub-iterator. **Thread 2** (~19:22-00:19): symbolic execution engine architecture for user's project — 2× 2000-bit BitVec store, 3 instruction types each with branching. Covered KLEE internals, Z3 expr cost model, DFS+incremental solving alignment, Branch Descriptor pattern, recursive vs iterative DFS, isolated vs accumulated guards, Flat Sibling SMT pattern, stepper-returns-ordered-guards rule

## Agent index

- PYTHON-YIELD-MECHANICAL-TRANSLATION (s00): generator → class implementing iterator protocol. Rules: (1) locals→`self` attrs, (2) `self.pc` tracks resumption point, (3) `__next__` is state dispatcher (`if/elif self.pc == N`), (4) `yield`→`return` after updating `self.pc`, (5) function end→`raise StopIteration()`. `yield from <expr>`→`while True: try: yield next(_iter); except StopIteration as e: _return_value = e.value; break`. Strict formal equivalence; compiler does the heavy lifting
- KLEE-ITERATIVE-EXECUTION (s00): single-process Executor loop manages pool of ExecutionState. Per-iteration: Searcher (Random Path / Coverage-Optimized) selects state → execute 1 LLVM bitcode instruction → on symbolic branch: fork (new ExecutionState in pool, NOT OS fork)
- KLEE-MEMORY-OPTIMIZATIONS (s00): (1) **Object-level COW** at MemoryObject granularity — forks share pointers; only writes duplicate. (2) **Persistent data structures** (functional `std::map`-like trees) for address→object mapping; fork = O(log N) metadata update. (3) **Pruning** on termination (destroy state, dec-refcount). (4) **Experimental state merging** when same PC + combinable constraints (often adds solver pressure). (5) **Expression sharing/internalization** via ConstraintManager — same symbolic expression = same pointer
- Z3-EXPR-COPY-IS-O1 (s00): `z3::expr` = wrapper around `Z3_ast*` raw C pointer (~`shared_ptr<Z3_ast>`). Copy = atomic ref-count increment. **Hash-consing:** Z3 enforces expression uniqueness — building `x+1` checks internal hash table first, returns existing node if structurally equal. Syntactic equality = pointer comparison = O(1)
- Z3-CONTEXT-FATAL-CAVEAT (s00): cheap copy ONLY holds within same `z3::context`. Per-state separate contexts require `Z3_ast_translate` for forks — deep AST copy, bottleneck. **Architectural recommendation:** single global `z3::context`; rely on ref-counting for isolation; separate `z3::solver` instances per state (cheap) OR incremental `push()`/`pop()`. Watch for ref-counted memory leaks (dead constraints retained = RAM climb)
- KLEE-SOLVER-CHAIN (s00): KLEE treats Z3/STP as **stateless oracle** (no incremental push/pop). Optimizations: (1) **Constraint independence** — slice path condition to constraints touching only branch's variables, drop unrelated. (2) **CEX cache** — every successful Z3 model saved in global cache shared across states; new query first plugs cached models → if any satisfies, bypass Z3 entirely. (3) **Canonical query caching** via hash-cons → O(1) hashmap lookup. **Reason KLEE avoids push/pop:** heuristic search requires constant context-switching between states; LCA backtracking thrashes solver's learned lemmas. Incremental mode requires strict stack discipline (DFS)
- DFS-PEEK-AND-MUTATE-PATTERN (s00): explicit stack frame `{state, explored_true, explored_false}` (Python) or `{state, fetched_branches, branches, next_branch_index}` (C++ Branch Descriptor variant). **PEEK not pop** — parent stays as anchor for solver pop. `solver.push()` synced with stack.append; `solver.pop()` synced with stack.pop (only if non-empty after). Zero solver thrashing (lemmas retained); max RAM = max tree depth (not path count). Perfect alignment guaranteed by `if len(stack) > 0` check
- BRANCH-DESCRIPTOR-PATTERN (s00): stepper returns `std::vector<SymbolicTransition>` where transition = `{z3::expr guard, std::vector<std::pair<VarID, z3::expr>> assignments, Address next_pc, optional std::function<void(state)> apply_effects}`. Stepper is **read-only** on state; engine decides which to take based on solver feasibility. Decouples instruction semantics from search strategy. Avoid C++20 coroutines for symbex (ownership/refcount nightmare with shared Z3 exprs). Engines using this: Crucible (Galois, Haskell, continuation-passing), CBMC, Microsoft Boogie/Corral, Z3 SPACER. **Maps cleanly to Lean 4/Rocq for verifying engine itself** (pure functions State→List[Transitions])
- STEPPER-EAGER-VS-LAZY-FORKING (s00): returning `vector<ExecutionState>` = **Eager Forking** (pays copy cost before SAT check; "Birth Certificate" problem — state needs `last_guard` field; logic coupled to fork()). Returning `vector<SymbolicTransition>` = **Lazy** (only fork after SAT). Lazy is purer (data-driven recipe), debuggable (printable updates), more refactor-friendly. Eager penalty minor for tiny states; ruinous at scale
- SYMBOLIC-STORE-PERSISTENT-DATASTRUCTURE (s00): **C++ gold standard: arximboldi/immer (HAMT)** for O(1)/O(log N) fork. Alternative: flat `std::map`/`std::unordered_map` — O(N log N) deep copy per fork = bottleneck at scale. Python: Angr forbids `.copy()`, uses CoW dicts with parent fallback chain (O(1) fork, O(D) lookup). **NOT relevant for user's 2× BitVec store** — copying 2 z3 pointers is O(1)
- PRUNING-PIPELINE-5-STAGES (s00): (1) **Constant folding in stepper** — if condition resolves concrete (`cond.is_true()`/`is_false()`), return only the feasible branch. (2) **`z3::expr.simplify()`** — algebraic reduction (`x ∧ ¬x → false`); discard if simplifies to false. (3) **Constraint independence** — if guard shares no variables with path condition π, check guard alone. (4) **CEX cache** — test new guard against cached concrete models. (5) **Full `solver.check()`** — only as nuclear option. Order strictly by cost. "Impossible guard is a gift" — Dead Code Elimination for execution tree
- RECURSIVE-DFS-PYTHON-VS-CPP (s00): Python — soft `RecursionError` at `sys.getrecursionlimit()` default 1000 (catchable, gracefully drop state, log path-too-deep). C++ — no built-in counter, OS thread stack: Linux/macOS main thread 8MB (~130k frames at 64 bytes/frame), Linux/macOS spawned threads ~512KB, Windows 1MB. **Failure mode: SegFault, uncatchable.** Configure via `ulimit -s` or linker `/STACK:`. **C++ requires manual `depth` counter** to fail gracefully before OS kills engine: `if (depth >= MAX_DEPTH) return;`. Combines hardware-stack reach with Python-style safe failure
- RECURSIVE-DFS-COPY-BOTTLENECK (s00): in recursive DFS, `state.copy()` / `ExecutionState child = state;` triggers store duplication. For large `std::map`/`dict` (10k vars): bottleneck. **NOT user's concern** — 2× BitVec store = copying 2 z3 pointers, O(1). Python `copy.deepcopy()` catastrophically slow; Angr uses CoW dict-chain. New watch instead: **AST tree depth growth** — sequential bit-vector mutations build deep `concat`/`extract` chains; solver chokes on `.check()` even though state copy stays cheap
- RECURSIVE-CPP-ARCHITECTURE-RAII-WIN (s00): RAII syncs with Z3 ref-counting: `ExecutionState child = state;` increments refs; destruction decrements. Perfect deterministic memory management for AST nodes without manual GC. Recursive C++ with stable stepper modules + dispatcher is concise & fast IF (a) state copy is cheap, (b) depth bounded, (c) modular steppers stay read-only with capture-by-value lambdas, concrete `next_pc` calc outside `apply_effect`
- BRANCH-GUARD-LOCAL-VS-EFFECTIVE (s00): isolated/local guard $I$ (what the branch says) vs accumulated/effective guard $A$ (what's actually reachable). $A_3 = \neg I_1 \land \neg I_2 \land I_3$ for fall-through `if`/`else if` chain. **Synonyms:** Compiler theory — "Reachability Condition." Symex — "Disjoint Path Constraint" (I-guards Overlapping → A-guards Mutually Exclusive). HW — "Priority Encoder" logic. SMT — RawGuard vs DisjointGuard. Classic symex — EdgeCondition vs PathConstraint. **Engine using only I-guards is UNSOUND** (e.g. `if x>10 else if x>5`: I_2-only solver might find x=20 which actually goes to block 1)
- BRANCH-GUARD-NAMING-RECOMMENDATION (s00): **`LocalGuard` / `EffectiveGuard`** ("Pragmatic Systems Pair") best for C++ readability. Alternatives: `BranchPredicate`/`ReachabilityPredicate` (formal verification), `RawGuard`/`DisjointGuard` (SMT), `EdgeCondition`/`PathConstraint` (classic symex). Match to project style
- FLAT-SIBLING-SMT-PATTERN (s00): **wrong:** `push I_1`→`pop`→`push !I_1`→`push I_2`→`pop`→`push !I_2`… ("staircase") — solver stack depth = N for N branches; CDCL hates deep nested scopes (lemma partitioning, backtrack table bookkeeping dwarfs solving). **Right:** outer `solver.push()` for entire chain; per-branch: inner `solver.push()` + `solver.add(local_guard)` → `solver.check()` → recurse if sat → `solver.pop()` (drop the +guard) → `solver.add(!local_guard)` *into outer scope*. **Max stack depth = 2 regardless of chain length.** Z3 natively acts as accumulator (flat list `[!I_1, !I_2, …]` enables unit propagation). No C++ `acc = acc && !I_i` needed
- STEPPER-CONTRACT-PRIORITY-ORDER (s00): steppers MUST return local guards in **priority order** (highest-priority `if` first, fall-through `true` last). Engine blindly applies Flat Sibling. **Division of labor:** stepper owns Semantic Priority (knows which branch is "first" in source order); engine owns Logical Accumulation (negates as it iterates). Fall-through MUST be present as final element with `true` guard so `¬I_1 ∧ ¬I_2 ∧ true` correctly models default branch. Hallmark of professional-grade formal verification tool
- INCREMENTAL-SOLVING-DFS-ALIGNMENT (s00): `push()`/`pop()` ideal for strict DFS (perfectly aligned with stack pop on backtrack). Useless for heuristic search (random-path / coverage-guided — context switching thrashes learned lemmas). **Verdict:** if committing to DFS-only, use incremental. If heuristic search needed (KLEE/Angr territory), use stateless oracle + caching/independence
- SE-ENGINE-DESIGN-USER-CONTEXT (s00): user building symex with 3 instruction types each with branching, symbolic store = 2× ~2000-bit BitVec. **Architectural fit:** recursive C++ + Branch Descriptor pattern + LocalGuard/EffectiveGuard naming + Flat Sibling SMT pattern + stepper-priority-order contract + manual depth counter. State copy O(1) (only 2 pointers); risk is AST tree depth on solver.check, not memory
